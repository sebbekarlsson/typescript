class Person {
    message: string;
    function say(message: string): void {
        print(message);
    };
};

function main(argc: number, argv: string[]): number {
    let person = new Person();
}
